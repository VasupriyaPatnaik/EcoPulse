{
  "name": "ecopulse-netlify",
  "version": "1.0.0",
  "description": "EcoPulse - Full Stack on Netlify",
  "scripts": {
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "dev": "netlify dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "serverless-http": "^3.2.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongoose": "^7.0.3",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "nodemailer": "^7.0.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0",
    "concurrently": "^8.2.2"
  }
}
